{
  "name": "personal-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-static.js",
    "dev": "python3 -m http.server 8000",
    "build:serve": "npm run build && cd dist && python3 -m http.server 8001"
  },
  "dependencies": {
    "@portabletext/to-html": "^3.0.0",
    "@sanity/client": "^6.18.2",
    "dotenv": "^16.4.5"
  }
}
